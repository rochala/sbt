name: Release native launchers
on:
  release:
    types: [published]
  workflow_dispatch:
  repository_dispatch:

jobs:
  ping-sbtn-dist:
    runs-on: ubuntu-latest
    steps:
    - name: Generate token
      uses: tibdex/github-app-token@v1
      id: generate-token
      with:
        app_id: ${{ secrets.APP_ID }}
        private_key: ${{ secrets.APP_PRIVATE_KEY }}
    - name: PING - Dispatch native script release event
      if: github.event.action != 'pong'
      run: |
        curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ steps.generate-token.outputs.token }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/rochala/sbtn-dist/dispatches \
          -d '{"event_type":"ping","client_payload":{"repository": \"'"$GITHUB_REPOSITORY"'\" }}'
    - name: ACK - Acknowledge pong from sbtn-dist
      if: github.event.action == 'pong'
      run: |
        echo "PONG received from '${{ github.event.client_payload.repository }}'" && \
        curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ steps.generate-token.outputs.token }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/rochala/sbtn-dist/dispatches \
          -d '{"event_type":"ack","client_payload":{"version": "${{ github.ref }}"}}'

